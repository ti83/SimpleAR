<UserControl x:Class="SimpleAR.Views.StatementView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="40"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <StackPanel Grid.Row="0" Orientation="Horizontal">
            <TextBlock Text="Start Date:" VerticalAlignment="Center" Margin="5"/>
            <DatePicker VerticalAlignment="Center" Margin="5" Height="30" VerticalContentAlignment="Center"/>
            <TextBlock Text="End Date:" VerticalAlignment="Center" Margin="5"/>
            <DatePicker VerticalAlignment="Center" Margin="5" Height="30" VerticalContentAlignment="Center"/>
            <Button Content="Generate Report" Padding="20,0" Margin="5" Background="LightGreen"></Button>
        </StackPanel>
        <ScrollViewer Grid.Row="1">
            <ItemsControl>

            </ItemsControl>
        </ScrollViewer>
    </Grid>
</UserControl>
